
{# app/Resources/views/number.html.twig #}
{%  extends 'base.html.twig'%}
{% block title %}
    Rekordy
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="{{ asset('css/ride/ride.css') }}">
{% endblock %}

{% block body %}
    {{ parent() }}

    <div class="loader"></div>
    <div class="container">
        <h4 class="pageHeader">Rekordy</h4>
        <div class="row">
            <div class="col-md-12">
                <ul class="nav nav-tabs" id="myTab" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" id="allTimeRecord" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true">Rekordy wszechczasów</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="monthRecord" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false">Rekordy miesiąca</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="weekRecord" data-toggle="tab" href="#contact" role="tab" aria-controls="contact" aria-selected="false">Rekordy tygodnia</a>
                    </li>
                </ul>
                {#<div class="tab-content" id="myTabContent">#}
                    {#<div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">a</div>#}
                    {#<div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab">b</div>#}
                    {#<div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab">c</div>#}
                {#</div>#}
            </div>
            <div class="col-md-12 tableArea">
                <h4 id="recordHeader">Top 10 rekordów</h4>
                <table class="table" id="recordTable">
                    <thead>
                        <tr>
                            <td>Lp.</td>
                            <td>Uczestnik</td>
                            <td>Czas przejazdu</td>
                            <td>Gokart</td>
                            <td>Data</td>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            </div>
        </div>
    </div>


{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/lap/lap.js') }}"></script>
{%  endblock %}
